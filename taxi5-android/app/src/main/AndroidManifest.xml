<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.muei.apm.taxi5">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".ui.MainActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.RegisterActivity"
            android:label="@string/activity_register_label"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/activity_login_label"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.ChangePasswordActivity"
            android:label="@string/activity_change_password_label"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.EditProfileActivity"
            android:label="@string/activity_edit_profile_label"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.ProfileActivity"
            android:label="@string/activity_profile_label"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.PaymentActivity"
            android:label="@string/activity_pago_label"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/activity_home_label"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.TrayectoActivity"
            android:label="@string/activity_trayecto_label"
            android:parentActivityName=".ui.HomeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.RutaActivity"
            android:label="@string/ruta_activity"
            android:parentActivityName=".ui.TrayectoActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.SearchActivity"
            android:label="@string/activity_search_label"
            android:parentActivityName=".ui.HomeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.HistoricalActivity"
            android:label="@string/activity_historical_label"
            android:screenOrientation="portrait" />

        <!-- METADATOS -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>
</manifest>